{
  "name": "static-site-builder",
  "version": "1.0.0",
  "description": "Simple static site with shared layouts and live reload",
  "scripts": {
    "dev": "node scripts/dev.js",
    "build": "node scripts/build.js",
    "start": "npm run dev"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^17.0.1"
  }
}
